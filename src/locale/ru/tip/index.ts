import { status } from './status';
import { sticky } from './sticky';

export const tip = {
  sticky,
  status,
  team: { kickParticipant: 'Исключить' },
  tournament: {
    createAssignmentSchema: 'Создать схему урока на основе этого турнира',
  },
};

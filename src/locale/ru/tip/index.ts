import { sticky } from './sticky';
import { status } from './status';

export const tip = {
  sticky,
  status,
  team: { kickParticipant: 'Исключить' },
  tournament: {
    createAssignmentSchema:
      'Создать схему урока на основе этого турнира',
  },
};

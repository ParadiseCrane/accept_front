import { Divider } from '@mantine/core';
import styles from '../TipTapEditor.module.css';

export const ToolbarDivider = () => {
  return <Divider orientation="vertical" className={styles.toolbar_divider} />;
};
